/*
you are creating a countdown app.
implement a program that uses a while loop to count down from 10 to 1 and prints each number.
*/
let count = 10;
console.log("Counting From 10 to 0 : ");
while (count >= 0) {
    console.log(count);
    count--;
}